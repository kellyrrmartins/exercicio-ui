pipeline {
    agent any

    stages {
    
        stage('Instalar dependencias') {
            steps {
              git branch: 'main', url: 'https://github.com/kellyrrmartins/exercicio-ui.git'
            }
        }
        stage('Instalar dependencias') {
            steps {
              sh 'npm install'
            }
        }
        stage('Executar Testes') {
            steps {
              sh 'NO_COLOR=1 npm run cy:run'
            }
        }
    }
}